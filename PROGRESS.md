# PROGRESS.md — читалка книг

Журнал этапов. Контекст и аудит: `READER_ANALYSIS.md`.

---

## Этап 1 — данные книги ✅ (коммит `187563c`)

- `data/books/index.json` — каталог (1 книга).
- `data/books/bremer/` — `meta.json`, `glossary.json`, `ch-01…ch-04.json`
  («Die Bremer Stadtmusikanten», Brüder Grimm, B1, 4 главы, 1182 слова, ~9 мин).
- `scripts/validate_books.py` — зелёный.

---

## Этап 2A — вид «Библиотека» ✅ (этот коммит)

### Сделано

- **`books.html`** (корень проекта) — шапка «Библиотека» + подзаголовок,
  кнопка «← Главная» на `index.html`, переключатель темы (🌙/☀️).
  Подключает `css/base.css`, `css/reader.css`, `js/library.js`.
  Функции `initTheme()` / `toggleTheme()` — инлайн в странице, как в `index.html`
  (та же дупликация, что и везде в проекте; `localStorage` обёрнут в `try/catch`).
- **`css/reader.css`** — новый файл, неймспейс `lib-` для библиотеки
  (`rd-` зарезервирован под ридер, этап 2B). Только var()-токены из `base.css`,
  отдельная секция светлой темы, адаптив на 720 px и 420 px.
- **`js/library.js`** — IIFE-модуль `Library` (`init`, `load`, `openBook`),
  zero dependencies. `fetch data/books/index.json` → сетка карточек:
  обложка-эмодзи, `title` + `titleRu`, автор, бейдж уровня, «~9 мин»,
  число глав, прогресс-бар, счётчик собранных слов.
- **Прогресс** читается из `localStorage`: `dm_book_pos:<id>`,
  `dm_book_words:<id>`. Пока их никто не пишет → 0 % и «0 слов» —
  это нормальный путь, не ошибка.
- **Кнопка** «Читать» / «Продолжить» / «Читать заново» (при 100 %) →
  пока `console.log`, ридера нет.
- **Явные состояния**: загрузка (спиннер), ошибка (текст + причина +
  кнопка «Повторить»), пустой каталог (+ «Повторить»). Ни одного
  «глухого» `.catch(() => {})`: ошибки идут в `console.error` и на экран.

### Проверено (headless Chrome, локальный `python -m http.server`)

| Сценарий | Результат |
|---|---|
| 360×640, тёмная | ок, одна колонка, без горизонтального скролла (`scrollWidth == clientWidth == 360`) |
| 320×640, тёмная | ок, заголовок в две строки, CTA во всю ширину |
| 1280×900, тёмная | ок, сетка `auto-fill minmax(300px, 1fr)` |
| Светлая тема (`data-theme="light"`) | ок на 360 и 320 |
| Прогресс 42 % + 7 слов (подложен в `localStorage`) | бар залит, кнопка «Продолжить», «7 слов» |
| `index.json` → 404 | состояние ошибки, «HTTP 404 File not found», «Повторить» |
| Сеть недоступна | состояние ошибки, «Failed to fetch» |
| `{"books": []}` | состояние «Пока ни одной книги» |
| Медленный ответ | состояние загрузки со спиннером |

### Решения

- **Контракт `dm_book_pos:<id>`** зафиксирован здесь, писать его будет этап 2B.
  Чтение терпимое: `{ percent: 0..100 }` (приоритет) → `{ chapter, para }`
  (оценка `chapter / book.chapters`) → просто число как процент.
  Мусор и битый JSON → 0 % + `console.warn`.
- **`dm_book_words:<id>`**: массив слов, либо `{ words: [...] }`, либо
  объект-словарь — считается длина/число ключей. Гибкость нужна потому, что
  формат «сборника слов» определится на этапе 3.
- Проценты считаются **на клиенте при рендере**, ничего не кешируется:
  карточка всегда отражает актуальный `localStorage`.
- Карточки строятся строкой-шаблоном с `esc()`, обработчики навешиваются
  через `addEventListener` по `data-book-id` (без inline-`onclick`,
  в отличие от старых файлов — новый код, старую привычку не тащим).
- Файлы уроков не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js` — без изменений.
- `?v=51` у новых ассетов проставлен вручную под текущую версию проекта;
  `bump_version.py`, `service-worker.js` и кнопка в сайдбаре — этап 5.

### Следующее

Закрыто этапом 2B (ниже).

---

## Этап 2B — ридер: текст и пагинация ✅ (этот коммит)

### Сделано

- **`js/reader.js`** — IIFE-модуль `Reader` (`open`, `close`, `next`, `prev`,
  `goTo`, `repaginate`), zero dependencies. Отдельной страницы нет: ридер живёт
  в `books.html` как второй вид, переключение библиотека ↔ ридер без перезагрузки
  (`#libView` / `#rdView`, `body.rd-open`). Кнопка «Читать» из 2A вместо
  `console.log` вызывает `Reader.open(id)`.
- **Загрузка**: `meta.json` + `glossary.json` параллельно (`Promise.all`),
  затем `ch-NN.json` текущей главы. Глоссарий лежит в `st.gloss` и пока не
  используется — он нужен 2C, чтобы не дозагружать словарь по тапу.
- **Рендер**: слово → `<span class="bw" data-t="<lowercase>">`, предложение →
  `<span class="bs" data-p="i" data-s="j">`, пунктуация и пробелы — голый текст.
  Внутри span'ов никакого HTML. Перевод предложения в DOM не кладётся: 2C возьмёт
  его из `st.chapter` по `p`/`s`. У `.rd-content` стоит `lang="de"` — иначе
  переносы считались бы по правилам `lang="ru"` со страницы.
- **Пагинация** — CSS multi-column: `.rd-viewport { overflow: hidden }`,
  `.rd-content { column-width: W; column-gap: 32px; column-fill: auto; height: H }`,
  листание `transform: translateX(-page * (W + 32))` с переходом 180 мс,
  `pages = round(scrollWidth / (W + gap))`. Ручного замера высоты нет.
- **Позиция — якорь `{ chapter, p, s }`** (первое видимое предложение).
  Восстановление: найти `.bs[data-p][data-s]`, посчитать колонку через
  `offsetLeft / (W + gap)`, прыгнуть на неё. Номер страницы как позиция
  не хранится. В `localStorage` пишется `{ chapter, p, s, percent }` —
  формат из этапа 2A, карточка в библиотеке показывает реальный процент.
- **Панели**: верхняя — «назад», название главы + `titleRu`, «стр. 3 / 12»;
  нижняя — прогресс-бар главы, кнопки «‹ Назад» / «Вперёд ›» и пустой
  `#rdSlot` под кнопки 2C/2D.
- **Границы глав**: вперёд с последней страницы → следующая глава с первой;
  назад с первой → предыдущая глава на последней странице. Смежные главы
  заранее не грузятся. На концах книги кнопки блокируются.
- **Перепагинация** по `resize` и `orientationchange` (debounce 120 мс),
  без анимации, с сохранением якоря.
- **Состояния** загрузки и ошибки — явные, поверх вьюпорта, с «Повторить»
  и «В библиотеку»; на время состояния кнопки листания заблокированы.
  Ни одного `.catch(() => {})`.

### Проверено (headless Chrome, локальный `python -m http.server`)

Прогон из библиотеки кликами по реальным кнопкам, 360×640:

| Проверка | Результат |
|---|---|
| Геометрия | `scrollWidth` = 688 = `pages*(W+gap) - gap` при W=328, pages=2 — совпало точно |
| Страницы непустые | предложения распределились `{стр0: 5, стр1: 3}` |
| Разметка | `<span class="bw" data-t="es">Es</span>`, HTML внутри span нет, пунктуация вне span |
| Горизонтальный скролл страницы | отсутствует (`scrollWidth == clientWidth == 360`) |
| Все 4 главы вперёд | 0→1→2→3, каждая открывается со стр. 1, «Вперёд» блокируется на 4/4 последней главы |
| Все 4 главы назад | 3→2→1→0, каждая открывается на **последней** странице (4/4, 3/3, 2/2), «Назад» блокируется на 1/1 первой |
| Смена размера (800×500, 1280×900, 360×900, 360×640) | якорь `{p:2,s:4}` не изменился ни разу, его колонка всегда равна текущей странице, возврат к 360×640 дал ровно «стр. 3 / 4» |
| Сохранённая позиция | `{"chapter":2,"p":2,"s":4,"percent":69}` — без номера страницы |
| Возврат в библиотеку | карточка показала «69% прочитано» и «Продолжить» |
| Повторный вход | глава 3, «стр. 3 / 4», тот же якорь |
| Десктоп 1200×800 | колонка текста ограничена 720 px и центрирована, панели на месте |
| Тёмная и светлая тема | ок на 360×640 и на десктопе |
| Ошибка (книги нет) | «Книга не открылась», `data/books/nope/glossary.json: HTTP 404`, «Повторить» + «В библиотеку» |

Ограничение проверки: в headless с `--virtual-time-budget` CSS-переходы не идут,
поэтому анимация листания снята отдельным прогоном в реальном времени.
Свайпы не проверялись — их нет, это 2C.

### Решения и пойманные грабли

- **Якорь не перевыводится при перепагинации.** Первая версия после каждого
  resize брала «первое предложение страницы» — и позиция уезжала назад к началу
  главы (стр. 3/4 → 1/4 за три ресайза). Теперь `goTo` различает три случая:
  обычное листание (якорь = первое предложение новой страницы), восстановление
  (`opts.anchor`) и перепагинация (`opts.keepAnchor` — якорь читателя не трогаем).
- **`[hidden]` не работает при `display: flex`.** `.rd-view` и `.rd-state`
  перекрывали библиотеку и текст соответственно; добавлены явные
  `.rd-view[hidden]` и `.rd-state[hidden] { display: none }`.
- **Ширину для шага листания берём у `.rd-content`, не у вьюпорта**: у вьюпорта
  есть padding, и `clientWidth` давал бы шаг на 40 px больше реальной колонки.
- **Шрифты Google Fonts приезжают после первой пагинации** и меняют число
  страниц, поэтому после рендера главы висит `document.fonts.ready → repaginate()`.
- **Модули объявлены через `const`** — это лексический глобал, а не свойство
  `window`: перекрёстные проверки сделаны через `typeof Reader !== 'undefined'`
  (проверка `window.Reader` молча не срабатывала).
- Открытие ридера кладёт запись в `history` (`#book=<id>`), `popstate`
  возвращает в библиотеку — аппаратная кнопка «назад» в Telegram и PWA ведёт
  себя ожидаемо, а не выкидывает со страницы.
- Отдельного `reader.html` намеренно нет: один документ — одна загрузка
  ассетов и мгновенное переключение видов.
- Не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js`, `tts.js`.

### Следующее

- **Этап 2C** — жесты и настройки чтения. Закрыто (ниже).
  Поправка к плану: тултип и перевод предложения в 2C **не входят** — это этап 3.
- **Этап 3** — тултип по слову и перевод предложения (движок `.word-tip`
  + `st.gloss`), сборник слов книги (`dm_book_words:<id>`).
- **Этап 4** — озвучка абзацев (Google TTS, как в уроках).
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.

---

## Этап 2C — жесты и настройки чтения ✅ (этот коммит)

### Сделано

- **Жесты** — делегирование на `.rd-viewport`, capture-фаза, только Pointer
  Events (`pointerdown` / `pointerup` / `pointercancel`). Именно pointer, а не
  `touch` + `click`: иначе один тап листал бы дважды.
  - тап по левому полю (18 % ширины) → страница назад;
  - тап по правому полю (18 %) → страница вперёд;
  - тап по центру → скрыть/показать панели;
  - свайп по горизонтали (> 40 px, < 300 мс) → листание;
  - вертикальный свайп и медленное перетаскивание → игнор;
  - второй палец (`!e.isPrimary`) отменяет жест — пинч не листает.
  - кнопки поверх текста (состояние загрузки/ошибки) жестом не считаются.
- **Тап по слову** (`.bw`) выигрывает боковые зоны листания: страница не
  листается, слово оставлено под тултип этапа 3.
- **Кнопки «‹ Назад» / «Вперёд ›»** оставлены — на десктопе мышью удобнее.
- **Скрытие панелей** — только класс `.rd-chrome-hidden` на `.rd-view` →
  `opacity`, `transform`, `pointer-events`. Панели остаются в потоке, размеры
  `.rd-viewport` не меняются, перепагинации нет.
- **Настройки чтения** — bottom sheet (кнопка «Aa» в `#rdSlot`), сохраняются в
  `localStorage` под `dm_reader_prefs` = `{ fs, lh, theme, margin, font }`:
  - размер шрифта — 5 ступеней (15/17/19/21/23 px);
  - межстрочный интервал — 3 (1.5 / 1.75 / 2);
  - тема страницы — системная / сепия / тёмная;
  - поля — 3 ступени (12/22/38 px + ширина колонки 860/720/580 px);
  - шрифт — системный (DM Sans) / serif (Georgia).
  Ряды строятся из таблицы `SHEET_ROWS`, состояние — `aria-pressed`.
  Закрытие: крестик, тап по подложке, Esc.
- **Тема ридера не конфликтует с глобальной**: внутри ридера все цвета идут
  через свой слой `--rd-*`, объявленный на `.rd-view`. «Системная» наследует
  глобальные токены (`data-theme`), «сепия» и «тёмная» переопределяют `--rd-*`
  локально через `data-rd-theme`. Переключение `toggleTheme()` на странице
  сепию не ломает.
- **Любая смена настройки** → `repaginate()` с сохранением якоря (`keepAnchor`).

### Проверено (headless Chrome, синтетические pointer-события)

| Проверка | Результат |
|---|---|
| Тап справа / слева (x=340 / x=20) | +1 / −1 страница |
| Свайп влево / вправо | +1 / −1 страница |
| Вертикальный свайп | страница и панели не изменились |
| Медленный свайп (420 мс) | не листает |
| Тап по слову в правой зоне («soll») | страница не изменилась |
| `pointerup` + следом `click` | сдвиг ровно на 1 страницу, не на 2 |
| Скрытие панелей | `pages` 2 → 2, вьюпорт 360×520 → 360×520, `pointer-events: none` |
| Тап по центру (попал в `.bw`) | панели вернулись |
| Пять настроек подряд (fs → lh → тема → поля → шрифт) | якорь `{p:2,s:2,w:27}` не изменился ни разу; колонка якоря = текущая страница после каждой; `pages` 4→6→7→7→8→7 |
| `dm_reader_prefs` | `{"fs":4,"lh":2,"theme":"sepia","margin":2,"font":"serif"}`, CSS-переменные применились |
| Глобальная тема | `data-theme` не менялся; после `toggleTheme()` фон ридера остался сепийным |
| Esc | шторка закрывается |
| Регрессия 2B | 4 главы вперёд и назад (назад — на последнюю страницу), три смены размера с сохранением якоря |
| Все три темы ридера, 360×640 и 1200×800 | ок |

Ограничение проверки: под `--virtual-time-budget` CSS-переходы не идут, поэтому
`opacity` скрытых панелей в замерах равна 1 — фактическое скрытие снято
отдельным прогоном в реальном времени (панели не видны, полосы под ними пустые).

### Решения и пойманные грабли

- **Соседняя страница просвечивала в полосе `padding`.** `overflow: hidden`
  режет по padding-box вьюпорта, а не по колонке: при широких полях (38 px) и
  зазоре 32 px левый край следующей колонки попадал в кадр (замер: 9 «чужих»
  слов на странице при 320 px). Теперь зазор считается в `measure()`:
  `gap = max(32, боковое поле + 8)`, и число страниц берётся точной формулой
  `round((scrollWidth + gap) / (W + gap))` — при большом зазоре на десктопе
  простое `round(scrollWidth / (W + gap))` ошибается на страницу.
- **Якорь теперь по слову, а не по предложению**: `{ chapter, p, s, w, percent }`.
  Длинное предложение занимает несколько страниц, и «первое предложение
  страницы» на таких страницах указывало на колонку назад — переход «назад в
  предыдущую главу» открывал стр. 3/4 вместо 4/4, а каждая перепагинация
  отбрасывала на страницу раньше. Поле `w` — дополнение к контракту из 2A;
  старые позиции без `w` читаются как раньше (падение до `.bs`).
- **Перепагинация после смены настройки — синхронно, без
  `requestAnimationFrame`.** В невидимой вкладке кадры не идут, и пересчёт
  отставал на один шаг (размер шрифта менялся, а страницы оставались старыми).
  `measure()` читает `clientWidth`/`scrollWidth`, а чтение форсирует layout с
  новыми переменными — этого достаточно. Второй проход остаётся на
  `document.fonts.ready` (переключение на serif и обратно).
- **Тап по слову в центре переключает панели.** Текст занимает почти весь
  центр, и если бы слово «съедало» и центральный тап, жест «скрыть панели» был
  бы недоступен. Слово выигрывает только боковые зоны листания. В этапе 3
  тултип перехватит центральный тап по слову раньше.
- **Шторка и подложка показываются классом `.show`, без атрибута `hidden`** —
  чтобы снова не наступить на `display: flex` против `[hidden]` (грабли 2B).
- Не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js`, `tts.js`.

### Следующее

- **Этап 3A** — тултип перевода по тапу на слово. Закрыто (ниже).
- **Этап 3B** — сборник слов книги (`dm_book_words:<id>`) и подсветка.
- **Этап 3C** — перевод предложения по тапу на `.bs`, кнопка в тултипе.
- **Этап 4** — озвучка абзацев (Google TTS, как в уроках), кнопка в `#rdSlot`.
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.

---

## Этап 3A — тултип перевода по тапу на слово ✅ (этот коммит)

### Сделано

- **`js/reader-tip.js`** — модуль `ReaderTip` (`open`, `close`, `isOpen`,
  `position`, `lookup`, `render`), zero dependencies. `js/lesson-render.js`
  не тронут: логика `mountWordTip` / `positionWordTip` / `openWordTip`
  (visualViewport, флип сверху/снизу, клампинг по краям, портал в body,
  стрелка через `--word-tip-arrow-left`) перенесена в свой модуль — у уроков
  свой инстанс на hover, у ридера свой на тап.
- **Вид** — переиспользован класс `.word-tip` из `css/lesson.css` (файл только
  подключён в `books.html`, не изменён) плюс модификатор `.word-tip--book`
  в `css/reader.css`.
- **Тултип один на документ**, лежит в `body`, содержимое рендерится лениво при
  открытии. Внутри `.bw` по-прежнему нет никакого HTML.
- **Данные** — `st.gloss`, загруженный ещё в 2B. Новый формат учтён: значение в
  `glossary.w` — либо строка-лемма, либо `{ l, old }`; помета `old` со
  словоформы показывается наравне с `old` леммы, при наличии обеих — обе.
- **Содержимое**: часть речи (kicker), слово и лемма через «→», если лемма
  отличается; перевод; существительное — артикль + мн. ч., при `pl: null` —
  «мн. ч. нет»; глагол — три формы (инфинитив + `forms`); прилагательное —
  степени сравнения; субст. прил. и n-Deklination — строка `decl`; прозвище —
  без грамматики; пометы «устар.» отдельными строками приглушённым цветом.
  Под кнопки 🔊 (этап 4) и «Предложение» (этап 3C) оставлен пустой
  `.word-tip-actions`, схлопнутый через `:empty`.
- **Тема тултипа**. Тултип живёт в `body`, слой `--rd-*` до него не доходит,
  поэтому тема ридера зеркалится в `data-rd-theme` на самом тултипе, а
  `.word-tip--book[data-rd-theme="sepia"|"dark"]` переопределяет ровно те
  токены, которыми пользуется `.word-tip`. «Системная» наследует глобальную
  тему страницы.
- **Жесты**: тап по `.bw` открывает тултип и забирает тап в любой зоне —
  страница не листается и панели больше не переключаются. Тап по пустому
  центру переключает панели как раньше. Повторный тап по тому же слову
  закрывает. Закрытие: тап вне, Esc, листание (через `goTo`), открытие
  настроек, закрытие ридера, `resize`/`orientationchange`.
- **Слова нет в глоссарии** → «нет в словаре» без падения (в книге таких форм
  две: `gefiel` и словоформа `geselle`).

### Проверено (headless Chrome, синтетические pointer-события)

Содержимое — на 16 словоформах разных типов:

| Слово | Что показал тултип |
|---|---|
| `ging` | гл. · ging → gehen · перевод · `gehen · ging · ist gegangen` |
| `ward` | гл. · ward → werden · три формы · **устар.** (помета со словоформы) |
| `Hause` | сущ. · Hause → Haus · `das Haus · мн. ч.: die Häuser` · **устар.** |
| `Besseres` | прил. · Besseres → gut · `gut · besser · am besten` |
| `Abgeschickte` | субст. прил. · строка `decl`, без артикля и мн. ч. |
| `Herr` | сущ. · `der Herr · мн. ч.: die Herren` + отдельная строка n-Deklination |
| `Musikanten` | сущ. · Musikanten → Musikant · артикль + мн. ч. + n-Deklination |
| `Packan` | прозвище · только перевод и **устар.**, без грамматики |
| `mehr` | нареч. · без грамматики, без стрелки (лемма совпадает) |
| `Asche` | сущ. · `die Asche · мн. ч. нет` (`pl: null`) |
| `viere` | числ. · viere → vier · **устар.** со словоформы |
| `alle` | мест. · alle → alle/alles |
| `war's` | гл. · war's → sein (токен с апострофом) |
| `gefiel`, `geselle` | «нет в словаре», без падения |
| синтетика: обе пометы | две строки **устар.** — со словоформы и с леммы |

| Проверка | Результат |
|---|---|
| Границы на 360×640 | все 96 слов страницы: 0 вылезаний; крайние случаи (первое, последнее, самое правое, самое левое, верхняя и нижняя строка) — в кадре, флип `word-tip-below` срабатывает на верхней строке |
| Границы на 1200×800 | все 182 слова страницы: 0 вылезаний |
| Тап по слову в боковой зоне | страница 0 → 0, тултип открыт |
| Тап по слову в центре | `chromeHidden` false → false, тултип открыт |
| Повторный тап по тому же слову | тултип закрылся |
| Тап по пустому центру | панели переключились (как в 2C) |
| Листание кнопкой при открытом тултипе | страница сменилась, тултип закрыт |
| Тап в боковую зону при открытом тултипе | тултип закрыт, страница **не** перелистнулась |
| Esc, открытие настроек | тултип закрыт |
| Три темы ридера | `data-rd-theme` зеркалится; сепия — кремовый фон `#ddd4bc`-ряда, «тёмная» при глобальной светлой теме остаётся тёмной |

### Решения и пойманные грабли

- **Ширина тултипа зависела от собственного `left`.** Тултип — shrink-to-fit
  между `min-width` и `max-width`: при большом `left` справа мало места и текст
  переносится иначе. Измерение шло до клампинга, поэтому ширина занижалась и
  правый край уезжал за экран — 10 слов из 96 на 360 px. Теперь перед замером
  `left` ставится в минимум (места максимум), и только потом считается позиция.
  Это же объясняет второй проход `positionWordTip` через rAF в уроках.
- **Закрытие «тапом вне» не должно листать.** Свой слушатель `pointerdown`
  в `ReaderTip` закрывал тултип раньше, чем жестовый слой ридера успевал
  увидеть его открытым на `pointerup` — тап по боковой зоне закрывал тултип
  **и** перелистывал страницу. Слушатель теперь не трогает тапы внутри
  `.rd-viewport`: там решение принимает ридер и тап «съедается».
- **`.word-tip--book` получил `pointer-events: auto`** (в базовом классе
  `none`): иначе тап проходил бы сквозь тултип на текст под ним, а в этапах 4
  и 3C там появятся кнопки.
- Тултип позиционируется по `offsetWidth/offsetHeight`, а не по
  `getBoundingClientRect`: у скрытого тултипа стоит `scale(.98)`, и rect
  занижал бы размеры на 2 %.
- `.bw.word-tip-open` подсвечивается акцентом ридера — это отклик на тап,
  а не «подсветка слов» из 3B.
- `css/lesson.css` подключён в `books.html` целиком: все его селекторы
  классовые и привязаны к разметке урока (`.lesson-*`, `.phrase-*`,
  `.vocab-*`), в `books.html` таких классов нет — конфликтов не возникает.
- Не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js`, `tts.js`.

### Следующее

- **Этап 3B** — сборник слов книги (`dm_book_words:<id>`), кнопка «добавить»
  в тултипе, подсветка собранных слов.
- **Этап 3B** — сбор слов и подсветка. Закрыто (ниже).
- **Этап 3C** — перевод предложения по тапу на `.bs` (данные уже есть в
  `st.chapter` по `p`/`s`), кнопка «Предложение» в `.word-tip-actions`.
- **Этап 4** — озвучка (Google TTS), кнопка 🔊 в `.word-tip-actions` и в `#rdSlot`.
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.

---

## Этап 3B — сборник слов и подсветка ✅ (этот коммит)

### Дыры в глоссарии не было

Задача начиналась с «починить покрытие: 521 против 501, нет `gefiel` и
`geselle`». Проверил — **расхождения между валидатором и ридером нет**:

- 521 насчитала **моя одноразовая проба в 3A** — регулярка
  `[A-Za-zÄÖÜäöüß]+` без склейки апострофа, поэтому `gefiel's` распадался
  на `gefiel` + `s`;
- `geselle` я выдумал сам: в тексте есть только `gesellen`;
- в глоссарии обе формы давно есть: `gefiel's → gefallen`,
  `gesellen → Geselle`;
- `validate_books.py` и `WORD_RE` из `reader.js` дают одно и то же:
  **501 словоформа, все покрыты, ошибок 0**.

Добавлять `gefiel` и `geselle` в `glossary.w` нельзя: этих форм в тексте нет,
и `validate_books.py:270` законно ругнулся бы на неиспользуемые ключи.

Но настоящее расхождение в регулярках было — просто латентное, на этой книге
не проявляется: валидатор и `build_book.py` не склеивали токен через дефис и
знали только латиницу с умлаутами, а ридер склеивает (`Süd-Ost` — одно слово) и
матчит любую букву Unicode. **Обе регулярки приведены к ридеру** (источник
правды — то, что попадает в `.bw`):

```python
# js/reader.js: /[\p{L}\p{M}]+(?:[-'’][\p{L}\p{M}]+)*/gu
# в Python нет \p{L}, поэтому [^\W\d_] — тот же класс «любая буква»
TOKEN_RE = re.compile(r"[^\W\d_]+(?:[-'’][^\W\d_]+)*", re.UNICODE)
```

Прогон после правки — цифры те же, значит на этой книге регулярки совпадали:

```text
books/bremer: глав 4, предложений 45, токенов 1182, словоформ 501, лемм 376

Проверено книг: 1. Ошибок: 0.
```

### Сделано

- **`js/reader-words.js`** — модуль `ReaderWords` (`load`, `save`, `all`,
  `get`, `count`, `isMarked`, `add`, `setStatus`, `remove`), zero dependencies.
- **Тап по слову = «не знаю»**: лемма пишется в `dm_book_words:<bookId>`,
  ключ записи — **лемма**, поэтому `ging` и `gingen` дают одну запись `gehen`:

  ```json
  { "gehen": { "de": "gehen", "ru": "…", "pos": "гл.", "forms": "ging · ist gegangen",
               "seen": ["gingen", "ging"], "n": 2,
               "ch": "ch-01", "p": 0, "ts": 1789369814095, "status": "new" } }
  ```

  `art`, `pl`, `forms`, `decl`, `old` кладутся только те, что есть в словаре
  (`pl: null` сохраняется — «мн. ч. нет» это факт, а не отсутствие данных).
  `ch`/`p` — где слово встретилось **впервые**, дальше не переписываются;
  `ts` — время последнего тапа; `n` — сколько раз спросили перевод.
- **Слова без словарной записи** («нет в словаре») в сборник не идут:
  `ReaderWords.add` возвращает `null`.
- **Подсветка** — класс `.bw--seen` на всех словоформах собранных лемм.
  Ставится при открытии книги и после рендера каждой главы (`applySeen`),
  а после тапа — сразу на **все** формы этой леммы в главе (`markLemma`),
  не только на нажатую. `status: 'known'` подсветку снимает, запись остаётся.
- **Счётчик в библиотеке** уже читал `dm_book_words` и считает ключи объекта —
  правок не потребовалось, число лемм показывается как есть.
- **Перечитывание не ломается**: тап по уже собранному слову открывает тултип
  как обычно, `n` растёт, дубля не появляется. Тап, который тултип **закрыл**,
  за «не знаю» не считается — для этого `ReaderTip.open` теперь возвращает
  `true`/`false` (открылся / закрылся).

### Проверено (headless Chrome, синтетические pointer-события)

| Проверка | Результат |
|---|---|
| Тап `gingen`, затем `ging` (в другой главе) | одна запись `gehen`, `n=2`, `seen=["gingen","ging"]`, ключей в хранилище 1 |
| Подсветка сразу после тапа | все `gingen` на странице — 2 из 2; `ging` подсвечен сразу |
| `pages` до и после тапа | 2 / 2 |
| Пагинация при 50 леммах / 105 подсвеченных слов | по всем главам `0:2 1:4 2:4 3:4` — совпало со состоянием без подсветки |
| Подсветка после листания, смены главы, `reload` | сохраняется (8 слов на первой странице ch-01 после перезагрузки) |
| Повторный тап по собранному слову | тултип открыт, `n` 1→2, ключей столько же |
| Тап-закрытие того же слова | `n` не вырос |
| Счётчик в библиотеке | «📗 3 слова» при 3 леммах в хранилище |
| `status: 'known'` | подсвечено 8 → 4, запись жива, слово больше не подсвечено |
| Слово без словарной записи | `add(...)` вернул `null`, ключей 3 → 3 |
| Битый JSON в `dm_book_words` | ридер открылся, текст отрендерен (8 предложений), сборник пуст, ошибок нет, библиотека показала «📗 0 слов» |
| Три темы ридера | пунктир виден и в системной, и в сепии, и в тёмной; разбивка строк во всех трёх одинаковая |

### Решения

- **Подсветка только через `text-decoration`** (`underline dotted`, 1 px,
  `text-underline-offset: 3px`). Ни `border-bottom`, ни `padding`, ни
  `font-weight` использовать нельзя: они меняют метрики строки, а значит и
  разбивку на страницы. Так «подсветка не ломает пагинацию» выполняется
  не проверкой, а конструкцией.
- **Форма → лемма считается через тот же `glossary.w`**, что и тултип
  (`ReaderTip.lookup`): одна точка правды, никакого второго индекса.
- `ReaderWords` хранит **весь сборник книги в одном ключе** и пишет его целиком
  при каждом изменении: записей десятки-сотни, JSON меньше килобайта на
  десяток слов — отдельный слой батчинга был бы преждевременным.
- Битый JSON и недоступный `localStorage` дают пустой сборник + `console.warn`,
  ридер продолжает работать.
- `ch`/`p` фиксируют первую встречу, а не последнюю: так запись отвечает на
  «где я это встретил», что полезнее для повторного чтения.
- Не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js`, `tts.js`.

### Следующее

- **Этап 3C** — перевод предложения. Закрыто (ниже).
- **Этап 4** — озвучка (Google TTS), кнопка 🔊 в `.word-tip-actions` и `#rdSlot`.
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.
- Экран сборника слов (список, фильтр «новые / выученные», переключение
  `status`) — отдельной задачей: сейчас `status` меняется только через API.

---

## Этап 3C — перевод предложения ✅ (этот коммит)

### Сделано

- **Кнопка «Предложение ⌄»** в `.word-tip-actions` (место было оставлено в 3A).
  Раскрывает русский перевод **внутри тултипа** — отдельного окна нет.
  Повторный тап сворачивает; каретка меняется на ⌃, `aria-expanded`
  переключается.
- **Данные** — `st.chapter.paragraphs[p].s[s].ru` по `data-p` / `data-s`
  ближайшего `.bs`; глава уже в памяти, в DOM перевод не дублируется.
- **Позиция пересчитывается после раскрытия** тем же `position()`, что и при
  открытии: тултип вырастает, и клампинг либо растит его вверх, либо
  переворачивает (`word-tip-below`). Отдельной логики для раскрытия нет.
- **Состояние не запоминается**: `open()` всегда ставит `expanded = false`,
  новое слово открывается свёрнутым.
- **Подсветка предложения** `.bs--active` — только фон и цвет
  (`color-mix` от `--rd-accent`, `border-radius`), без метрик, как `.bw--seen`
  в 3B: разбивка на страницы измениться не может.
- **Закрытие тултипа снимает и раскрытие, и подсветку**: `close()` чистит
  `.bs--active`, `expanded`, `sentenceRu`. Значит листание (через `goTo`),
  Esc, тап вне, открытие настроек и выход из ридера гасят всё сразу.
- Кнопка 🔊 этапа 4 встанет в тот же `.word-tip-actions` рядом с «Предложением».

### Проверено (headless Chrome, два полных прогона подряд)

| Проверка | Результат |
|---|---|
| Перевод против `ch-NN.json`, 5 предложений | все 5 совпали символ в символ (`p0.s0`, `p0.s1`, `p1.s0`, `p1.s2` и разорванное `p1.s1`) |
| Предложение, разорванное между страницами | `p1.s1`, колонки слов `0,0,0,0,1,1,1,1,1` — перевод тот же и полный |
| Новое слово | открывается свёрнутым, блок скрыт |
| Тап по кнопке | развёрнуто, `.bs--active` = 1, каретка ⌃, `aria-expanded=true` |
| Повторный тап по кнопке | свёрнуто, `.bs--active` = 0 |
| Другое слово после раскрытия | открылось свёрнутым, подсветка снята |
| Раскрытие на слове нижней строки | тултип вырос вверх: y 382–506 → 325–506, целиком в кадре |
| Раскрытие на слове верхней строки | перевернулся вниз (`below=true`), y 117–336, в кадре |
| `pages` до и после раскрытия | 2 → 2 в обоих случаях; с активным предложением тоже 2 |
| Листание | тултип закрыт, `.bs--active` = 0 |
| Esc | тултип закрыт, `expanded=false`, `.bs--active` = 0 |
| Три темы ридера | перевод и `.bs--active` идут от темы: сепия — коричневый акцент `rgb(122,74,18)`, системная и тёмная — золотой `rgb(232,197,71)` |

### Пойманное по ходу

- **Ложная тревога в тестах, а не в коде.** Первый прогон показал пустой
  перевод на одном предложении. Разбор: `isOpen=false` сразу после тапа, но
  кнопка в тултипе есть — значит тултип успел открыться и закрыться.
  Изолированная проба того же тапа проходила. Оказалось, что падал **всегда
  первый тап харнесса** независимо от предложения, и хватило одного тика после
  `Reader.goTo`, чтобы всё сходилось. Это гонка синтетических pointer-событий
  под `--virtual-time-budget`; в продукте менять нечего — два полных прогона
  на чистых файлах дали 5 из 5.
- **Под `--virtual-time-budget` CSS-переходы не идут** (знакомо с 2B):
  computed `opacity` тултипа остаётся 0, и скриншот, снятый в момент
  незавершённого fade-in, выглядит полупрозрачным. Подтверждено замером
  (`opacity=0`, `visibility=hidden` в виртуальном времени) и пересъёмкой по
  одному кадру в реальном времени — там тултип полностью непрозрачный.
- `.word-tip-sentence` показывается через атрибут `hidden`, поэтому рядом сразу
  лежит `.word-tip-sentence[hidden] { display: none }` — грабли 2B
  (`display` перебивает браузерное `[hidden]`) второй раз не наступили.
- Слушатель кнопки — делегированный, навешен на узел тултипа один раз в
  `ensureTip()`: содержимое тултипа перерисовывается при каждом открытии,
  и слушатель на самой кнопке приходилось бы переподключать.
- Не тронуты: `lesson-render.js`, 68 оболочек, `gen_template.html`,
  `css/lesson.css`, `css/exercises.css`, `flashcards.js`, `tts.js`.

### Следующее

- **Этап 4A** — `onEnd` в `js/tts.js`. Закрыто (ниже).
- **Этап 4B** — озвучка в ридере: кнопка 🔊 в `.word-tip-actions` рядом с
  «Предложением» и озвучка абзаца кнопкой в `#rdSlot`.
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.
- Экран сборника слов (список, фильтр «новые / выученные», переключение
  `status`) — отдельной задачей.

---

## Этап 4A — onEnd в js/tts.js ✅ (этот коммит)

Ридер не тронут: в этом этапе менялся только `js/tts.js`, чтобы очередь чанков
на этапе 4B строилась на событии завершения, а не на оценке длительности.

### Сделано

- **`TTS.speak(text, opts)` принимает `opts.onEnd`** — колбэк вызывается
  **ровно один раз** с `{ ok, reason }`:

  | reason | когда |
  |---|---|
  | `end` | нормальное завершение (`ok: true`) |
  | `interrupted` | прервано новым `speak()` или `TTS.stop()` (`ok: false`) |
  | `error` | голос заговорил и сломался (`ok: false`) |
  | `no-audio` | все источники `<audio>` молчат (`ok: false`) |
  | `empty` | озвучивать нечего, текст пустой (`ok: false`) |
  | `timeout` | сработала аварийная страховка (`ok: true`) |

- **Один вызов `speak()` = одна сессия** (`newSession`). Каскад
  `speechSynthesis → Web Audio → <audio>` сессию не закрывает: `onEnd`
  отдаётся на итоговом исходе, а не на каждом шаге каскада. Реализовано
  во всех трёх ветках:
  - `speechSynthesis` → `utterance.onend` (+ флаг `abandoned`: если ушли на
    аудио-путь по таймауту или ошибке, `onend` от `cancel()` игнорируется —
    иначе сессия закрылась бы раньше времени);
  - Web Audio → `source.onended` (рядом с прежним сбросом `currentSource`);
  - `<audio>` → `addEventListener('ended')`; элемент синглтон, поэтому
    слушатель ровно один и снимается в `finish()` / `stopCurrent()`.
- **Аварийная страховка**: если событие завершения не пришло за
  `(300 + букв × 70) × 1.6` мс — `onEnd` вызывается принудительно с
  `reason: 'timeout'`. Когда реальная длительность известна (буфер Web Audio,
  `<audio>.duration`), окно расширяется до `max(база, длительность × 1.6 + 500)`
  — чтобы страховка не срабатывала раньше конца длинной фразы. Это именно
  страховка: в норме её снимает событие.
- **`TTS.stop()`** — останавливает текущее воспроизведение (тот же
  `stopCurrent`) и отдаёт `onEnd` с `{ ok: false, reason: 'interrupted' }`.
- `speakPhrase(text, spans, opts)` и `speakSlow(text, opts)` прокидывают
  третий/второй аргумент в `speak` — понадобится ридеру.

### Обратная совместимость

Без `onEnd` сессия не делает **ничего**: ни таймера страховки, ни слушателя
`ended` (`arm()` и `attachEnded()` выходят сразу, если колбэка нет). Старые
вызовы идут прежним путём.

Проверка — трассировка на **реальном уроке** `lessons/a1/lesson-01/`
(клик по `.word-speak`, клик по кнопке 🔊 `speakPhraseEl` со `spans`,
`speakSlow`) с детерминированными заглушками сети и звука. Записывались все
обращения к `speechSynthesis`, `fetch`, `AudioContext`, `<audio>` и **все
`setTimeout` из `tts.js`** — то есть точное расписание караоке-подсветки.
Трассы старого (`git show HEAD:js/tts.js`) и нового файла сравнивались `diff`:

| Ветка | Что покрыто | Результат |
|---|---|---|
| `speechSynthesis` (голоса есть) | utterance rate/pitch/lang/voice, фолбэк-таймер 700/1800 мс | трассы идентичны |
| Web Audio (голосов нет) | `fetch` прокси, `decodeAudioData`, `createBufferSource`, `start(0)`, кэш буфера | трассы идентичны |
| `<audio>` (нет `AudioContext`) | перебор источников: прокси → `translate.google.com`, `load`/`play`, подсветка от `el.duration` | трассы идентичны |

Караоке отдельно проверено на карточке с 5 словами («Mein Name ist Peter
Müller»): все 11 таймеров (5 пар вкл/выкл + финальный `hlClear`) — 50, 194,
194, 445, 445, 805, 805, 984, 984, 1200, 1500 мс — совпали с прежними до
миллисекунды, свечение на словах появляется. Ошибок в консоли урока: 0.

### Проверено (новое поведение, три ветки × девять сценариев)

| Сценарий | speechSynthesis | Web Audio | `<audio>` |
|---|---|---|---|
| Нормальное завершение | `{ok:true,end}` ×1 | `{ok:true,end}` ×1 | `{ok:true,end}` ×1 |
| Прерывание новым `speak()` | первый `interrupted`, второй `end` | то же | то же |
| `TTS.stop()` | `{ok:false,interrupted}` | то же | то же |
| `stop()` дважды + позднее событие | ровно одна запись | ровно одна | ровно одна |
| Пустой текст | `{ok:false,empty}` | то же | то же |
| Все источники молчат | (не доходит до сети) | `{ok:false,no-audio}` | `{ok:false,no-audio}` |
| Страховка, событие не пришло | 1936 мс = `(300+13×70)×1.6` | 2504 мс = `1.25с×1.6+500` | 2904 мс = `1.5с×1.6+500` |
| Вызов без `onEnd` | 0 колбэков, исключений нет | то же | то же |
| `onEnd` бросил исключение | следующий `speak` работает | то же | то же |

### Следующее

- **Этап 4B** — озвучка в ридере на базе `onEnd`. Закрыто (ниже).
- **Этап 5** — интеграция: кнопка «Библиотека» в сайдбаре и на главной,
  `books.html` и новые ассеты в `service-worker.js`, `python bump_version.py`.

---

## Этап 4B — озвучка в ридере ✅ (этот коммит)

`js/tts.js` не менялся: настоящего бага в нём не нашлось, хватило `onEnd`
из 4A.

### Сделано

- **`js/reader-speak.js`** — модуль `ReaderSpeak` (`chunkForTts`, `word`,
  `sentence`, `page`, `run`, `stop`, `isPlaying`, `toast`), zero dependencies
  кроме `TTS`.
- **Очередь идёт по `onEnd`**, а не по таймеру: следующий кусок стартует из
  колбэка завершения предыдущего. Счётчик `gen` гасит висящие колбэки
  прерванной озвучки — они сравнивают свой токен с текущим и молча выходят.
- **`chunkForTts(text)`** — нарезка под лимит прокси (200 символов):
  резы по границам клауз (`, ; : — –` и союзы `dass/weil/und/aber/denn/oder/
  sondern` — перед союзом), затем жадная склейка до 200. Куски сохраняют свою
  пунктуацию и пробелы, поэтому **склейка чанков даёт исходную строку символ
  в символ**, а слово не рвётся никогда (кусок всегда заканчивается пробелом).
  Слово длиннее 200 символов — единственный случай реза внутри слова, с
  `console.warn`; в живом тексте таких нет.
- **🔊 в тултипе** (`.word-tip-actions`) озвучивает **словоформу как в тексте**
  (`ctx.word`, не лемму), `rate: 0.9`. Вторая 🔊 — у раскрытого перевода,
  озвучивает предложение целиком (`de` из `st.chapter` по `data-p`/`data-s`).
  Сам тултип про TTS не знает: он сообщает о нажатии через `opts.onAct`, а
  состояние «играет» ему ставит ридер через `ReaderTip.setSpeaking()`.
- **«🔊 страница»** в `#rdSlot` читает предложения текущей страницы подряд
  (те, что на ней **начинаются**), подсвечивая текущее через `.bs--playing`.
  Дочитала — остановилась, страницу не листает. Повторный тап останавливает
  через `TTS.stop()`. На экранах ≤420 px подпись «страница» скрыта, остаётся
  значок — иначе нижняя панель не влезает.
- **Состояние кнопок**: `.is-playing` + пульсация + `aria-pressed` на кнопке
  страницы, `.is-playing` на 🔊 в тултипе — видно, что тап сработал.
- **Прерывания**: `next()` / `prev()` (листание, в том числе жестами),
  `loadChapter()` (смена главы), `close()` (выход из ридера), `openSheet()`
  (настройки) и любой новый запрос озвучки — всё останавливает звук и снимает
  подсветку.
- **Ошибка** → тихий тост «Озвучка недоступна» (класс `.dm-toast` из
  `base.css`), без `alert` и модалок; очередь закрывается штатно и не висит.
- `.bs--playing` — только фон и цвет, как `.bs--active` и `.bw--seen`:
  метрики строки не меняются, пагинация не может поехать.

### Проверено (headless Chrome, настоящий `tts.js` с подменой сети и звука)

| Проверка | Результат |
|---|---|
| `chunkForTts` на самом длинном предложении книги (461 симв., `ch-04 p3 s0`) | 3 чанка [200, 179, 82], все ≤200, склейка == исходник, каждый кусок кроме последнего кончается пробелом |
| Все 45 предложений книги | чанков >200 — 0, склейка сломалась — 0, слово порвано — 0 |
| Краевые случаи | пустая строка → 0 чанков; ровно 200 → 1; 201 без разделителей → режется по пробелам; слово 260 символов → 2 куска (с warn) |
| Очередь по `onEnd`, не по таймеру | заглушка играет длинные куски **быстрее** коротких: порядок кусков совпал с порядком чанков, интервалы равны заданным длительностям (120/120 мс), наложений 0, `onDone {ok:true,end}` |
| Чтение страницы | 4 предложения, подсветка прошла `["0.0","0.1","1.0","1.1"]` — ровно порядок `pageSentences()`; `pages` 2 → 2; по концу страницы кнопка погасла, `.bs--playing` снят, страница не перелистнулась |
| Повторный тап по «🔊 страница» | играет → не играет, подсветка снята |
| Листание во время чтения | звук остановлен, подсветка снята, новых кусков после листания — 0 |
| 🔊 слова | сыграна словоформа «Es», кнопка светилась сразу после тапа и погасла по завершении |
| 🔊 предложения | 1 кусок, `.bs--playing` = 1 во время и 0 после, кнопка меняла состояние |
| `reason: 'no-audio'` (сеть и все источники молчат) | тост «Озвучка недоступна», модалок нет, `isPlaying=false`, следующая озвучка работает |
| `reason: 'error'` (голос заговорил и сломался) | то же: тост, очередь не висит, следующая озвучка работает |
| Уроки | слово, фраза и `speakSlow` — по одному запросу каждый, караоке светит (3 из 5 слов в момент замера), ошибок на странице 0, `ReaderSpeak` в уроке вообще не подключён |

### Пойманное по ходу

- **`speakStop()` нельзя вешать на `goTo()`.** Сначала я поставил остановку
  туда — и чтение обрывалось само: `goTo` вызывает не только листание, но и
  `repaginate()` (resize, смена настроек, поздняя `document.fonts.ready`).
  В тестах это выглядело как `onDone {ok:false, interrupted}` на середине
  предложения. Теперь остановка висит на действиях читателя: `next`/`prev`,
  `loadChapter`, `close`, `openSheet`. Перепагинация чтение не рвёт — DOM
  тот же, подсветка остаётся валидной.
- **`books.html` не подключал `js/tts.js`** — без этого ридер упал бы на
  первой же попытке озвучки. Подключён перед `library.js`.
- Заглушки сети и звука надо ставить **до первого клика**: `unlock()` в
  `tts.js` кэширует `AudioContext` и `<audio>` на первом жесте, и после этого
  подменять их поздно. И перехватывать в фейке `fetch` только запросы
  озвучки — иначе не загрузится сам каталог книг.

### Следующее

- **Этап 5A** — точка входа в читалку. Закрыто (ниже).
- **Этап 5B** — версии, service worker, синхронизация. Закрыто (ниже).
- Экран сборника слов (список, фильтр «новые / выученные») — отдельной задачей.

---

## Этап 5A — точка входа в читалку ✅ (этот коммит)

Версии `?v=`, `service-worker.js` и синк — этап 5B.

### Сделано

- **`openBooksModal()` больше не показывает заглушку**, а переходит в
  `books.html`. Имя функции сохранено: её вызывают 68 оболочек уроков через
  inline-`onclick`, и менять их нельзя.
- **Путь считается по `<link rel="manifest">`**: в корне это `manifest.json`,
  в уроке — `../../../manifest.json`, значит базовый префикс получается
  отбрасыванием имени файла. Отдельной константы базового пути в проекте нет,
  а манифест есть на каждой странице — поэтому он и служит опорой.
  Новые функции: `dmBasePath()`, `dmBooksUrl()` (экспортирована в `window`
  рядом с `openBooksModal`).
- **Пункт сайдбара переименован** в «Книги на немецком» / «Чтение с переводом
  и озвучкой», иконка 📖 осталась. В `index.html` текст исправлен статически.
  В 68 оболочках он прошит в HTML, а править их нельзя — поэтому
  `dmFixBooksItem()` переписывает подпись при загрузке страницы
  (ищет `.side-action` с `openBooksModal` в `onclick`). Тот же механизм
  поправит и уроки, которые в будущем сгенерируются из `gen_template.html`.
- **Мёртвый код убран**: разметка модалки жила в теле `openBooksModal()` и
  ушла вместе с ним. Из `css/base.css` удалено правило `.dm-net-badge` —
  проверил по всему репозиторию, оно использовалось только в этой модалке.
  Остальные классы модалок (`.dm-emoji`, `.dm-title`, `.dm-text`, `.dm-note`,
  `.dm-btn`) нужны донат-модалкам и оставлены.
- **Карточки/кнопки книг на главной, кроме пункта сайдбара, нет** —
  `openBooksModal` встречается в `index.html` ровно один раз, поэтому
  «приводить в рабочий вид» больше нечего.

### Проверено

Переходы проверены кликом по реальному пункту сайдбара, с чтением
`location.pathname` после перехода:

| Откуда | `manifest` | `dmBooksUrl()` | Переход | Обратно |
|---|---|---|---|---|
| `index.html` | `manifest.json` | `books.html` | → `/books.html`, библиотека отрендерилась | → `/index.html` |
| `lessons/a1/lesson-01/` | `../../../manifest.json` | `../../../books.html` | → `/books.html` | → `/index.html` |
| `lessons/b2/lesson-14/` | `../../../manifest.json` | `../../../books.html` | → `/books.html` | → `/index.html` |

- Подпись пункта на всех трёх страницах: «Книги на немецком» /
  «Чтение с переводом и озвучкой», иконка 📖 (в уроках — после
  `dmFixBooksItem`).
- Модалка-заглушка не показывается ни на одной из страниц.
- **Тот же прогон под префиксом GitHub Pages** (сайт отдаётся из
  `/deutsch-meister/`): все три перехода ведут на
  `/deutsch-meister/books.html`, возврат — на `/deutsch-meister/index.html`,
  прямой заход на `/deutsch-meister/books.html` рендерит каталог.
- Коды ответов под префиксом: `/deutsch-meister/books.html` — 200,
  `/deutsch-meister/data/books/index.json` — 200,
  `/deutsch-meister/js/reader-speak.js` — 200.
- **68 оболочек, `gen_template.html`, `lesson-render.js`, `css/lesson.css`,
  `css/exercises.css`, `flashcards.js`, `tts.js` — без изменений**
  (`git status --porcelain` по этим путям пуст). Диф этапа:
  `css/base.css`, `index.html`, `js/support.js`.

### 404.html

Менять не потребовалось. `books.html` — настоящий файл в корне репозитория,
GitHub Pages отдаёт его напрямую (200), до `404.html` дело не доходит.
Единственный нюанс: адрес **без расширения** (`/deutsch-meister/books`)
даёт 404 и, значит, редирект `404.html` на главную — но ни одна ссылка в
приложении такой формы не использует, везде `books.html`.

### Следующее

- **Этап 5B** — версии `?v=` (через `python bump_version.py`), новые ассеты
  читалки в `service-worker.js` (`books.html`, `reader.css`, `reader*.js`,
  `data/books/**`) и синк прогресса/сборника слов, если он нужен.
- Экран сборника слов (список, фильтр «новые / выученные») — отдельной задачей.

---

## Этап 5B — версии, service worker, синхронизация ✅ (этот коммит)

Повод: после 4A у пользователя пропал звук в уроках — код был исправен, но
service worker отдавал старую копию `js/tts.js`, потому что `?v=` и `CACHE`
не бампались.

### 1. Версии

- `bump_version.py`: `html_files()` теперь включает **`books.html`** (было
  только `index.html`, `404.html` и 68 оболочек) — иначе ассеты читалки
  оставались на старой версии.
- `scripts/validate_lessons.py`: `check_versions()` сканирует тот же список,
  включая `books.html`, — иначе рассинхрон не поймался бы проверкой.
- Прогон: **версии 51 → 52, CACHE v66 → v67**, изменено 71 файл из 72
  (68 оболочек + `index.html` + `404.html` + `books.html`, плюс
  `service-worker.js`). После теста подхвата версий (см. ниже) итог —
  **?v=53 и CACHE v68**; `validate_lessons.py` зелёный, `validate_books.py`
  зелёный.
- В оболочках уроков изменились **только номера `?v=`** (952 строки, ничего
  кроме версий) — правил их не руками, а `bump_version.py`, как требует README.

### 2. Service worker

- В `STATIC` добавлены `books.html`, `css/reader.css`, `js/library.js`,
  `js/reader.js`, `js/reader-tip.js`, `js/reader-words.js`,
  `js/reader-speak.js`, `data/books/index.json`.
- `books.html` и `data/books/index.json` — **без `?v=`**: `bump_version.py`
  версионирует только `.js`/`.css`, а запросы идут network-first, так что
  свежее приходит из сети.
- **Главы и `glossary.json` не прекешируются** — библиотека будет расти.
  Они попадают в кеш на первом успешном чтении (обычная ветка `fetch`) и
  оттуда отдаются без сети.
- Добавлен список `NO_STORE` (`data/books/*/raw.txt`, `tools/sources/**`):
  исходники книг — материалы сборки, в кеш не идут.

### 3. Синхронизация

- `Progress.exportAll()` отдаёт поле **`books` верхним уровнем**, рядом с
  `progress` / `sections` / `lastLesson`; `dm_progress` не тронут. Формат:
  `{ <bookId>: { pos: {...}, words: { <лемма>: {...} } } }` — собирается из
  `dm_book_pos:*` и `dm_book_words:*`.
- Слияние (рядом с `_mergeGlobal`, но отдельными функциями, чтобы книги
  не попадали в `dm_progress`): позиция — **last-write-wins по `ts`**
  (запись без `ts` считается самой старой); слова — **union по лемме**,
  `n = max`, `seen` — объединение форм без дублей по регистру, **`known`
  побеждает `new`**, `ts = max`, `ch`/`p` (первая встреча) остаются
  локальные. Вызывается из `importAll`.
- `js/reader.js`: в `dm_book_pos:<id>` добавлено поле **`ts`** — без него
  last-write-wins не имел бы опоры.
- `api/webhook.py`: узкая ветка `_merge_books` при записи — остальной снимок
  по-прежнему перезаписывается входящим. Ценой одного лишнего `GET` на
  запись: иначе клиент без поля `books` (старая версия) стёр бы чтение,
  накопленное на другом устройстве.

### Проверено

| Проверка | Результат |
|---|---|
| `bump_version.py` | 51 → 52, CACHE v66 → v67, 71 файл; повторный прогон 52 → 53, CACHE v67 → v68 |
| `validate_lessons.py` / `validate_books.py` | 0 ошибок, 0 предупреждений; `?v=` согласованы (единственное значение — 53) |
| Оболочки уроков | диф только по `?v=`, правил `bump_version.py` |
| SW: прекеш | 25 записей `STATIC`, все файлы существуют на диске; в списке есть все 8 ассетов читалки; глав и `glossary.json` в прекеше нет; у `.json` нет `?v=` |
| SW: `activate` | старые кеши удаляются, остаётся только текущий |
| SW: cache-on-read | глава `ch-01.json` попадает в кеш на первом чтении; офлайн отдаётся из кеша с настоящим содержимым |
| SW: офлайн, непрочитанная глава | из кеша не выдаётся (ридер покажет штатную ошибку с «Повторить») |
| SW: `NO_STORE` | `tools/sources/bremer.txt` и `data/books/*/raw.txt` в кеш не попали |
| SW: чужой origin | `respondWith` не вызывается — запросы озвучки идут мимо воркера |
| **Подхват новой версии** | в старом кеше лежит устаревший `js/tts.js` → приходит SW с новым `CACHE` → `activate` удаляет старый кеш → запрос по новому `?v=` возвращает **новый** файл (маркер на месте), старая копия не отдаётся; офлайн из нового кеша тоже приходит свежая версия |
| Звук в уроках после обновления | `tts.js?v=53` подключается, тап по слову и по фразе дают запросы озвучки, караоке светит 3 из 5 слов, ошибок на странице 0 |
| Читалка после бампа | 8 предложений отрендерено, все шесть модулей на месте, все ассеты `?v=53`, ошибок 0 |
| Merge (клиент, `Progress.importAll`) | старый снимок без `books` ничего не меняет и не падает; снимок с `books` при пустом локальном пишется как есть; конфликт по лемме — `n=5`, `seen=[ging, gingen]`, `status=known`, леммы объединились; локальная свежая позиция сохраняется; битый JSON в `dm_book_words` не ломает ни экспорт, ни merge |
| Merge (сервер, `_merge_books`) | те же три случая + мусор вместо структуры (`None`, `[]`, строка, «dict-мусор») merge не ломает |
| `exportAll` | верхний уровень `progress, sections, lastLesson, books`; в `dm_progress` поля `books` нет |

### Ограничение проверки

**Браузерный офлайн-тест провести не удалось**: в этом headless Chrome
`caches.open()` падает с `UnknownError: Unexpected internal error`, поэтому
service worker не устанавливается вообще (`install` → `redundant`). Это
свойство окружения, а не кода: воркер из `HEAD` (до правок) ведёт себя точно
так же, квота при этом видна и `indexedDB` работает. Поэтому поведение
воркера проверено прогоном самого `service-worker.js` в песочнице Node с
фейковыми `caches`/`fetch` — включая cache-on-read, офлайн-фолбэк, `NO_STORE`
и сценарий с устаревшей копией `js/tts.js` в старом кеше. Живой офлайн стоит
проверить на устройстве: открыть книгу, прочитать главу, включить
авиарежим и открыть её снова.

### Следующее

- Экран сборника слов (список, фильтр «новые / выученные», переключение
  `status`) — сейчас `status` меняется только через API.
- Вторая книга в библиотеке: `scripts/build_book.py` + `validate_books.py`
  готовы, каталог и кеш масштабируются (главы кешируются по мере чтения).

---

## Этап 6 — три правки по следам чтения ✅ (эти три коммита)

Три независимые задачи, каждая своим коммитом: `92851e4`, `89a59f6`, `f8a9f33`.

---

### 6.1 Тултип не закрывает читаемый текст (`92851e4`)

**Повод.** При озвучке предложения тултип перевода накрывал сам текст, и
читатель не мог следить глазами за тем, что звучит. Прозрачность не
годится: буквы поверх букв читаются хуже полного перекрытия.

**Сделано.**

- **`js/reader-tip.js`** — `collapse()` / `restore()` / `isCollapsed()`.
  Прячется **только узел** (снимается `.word-tip-visible`); `target`,
  `expanded`, текст перевода и подсветка `.bs--active` остаются, содержимое
  не перерисовывается — поэтому возврат ничего не теряет и не мигает новым
  layout-ом. `close()` сбрасывает флаг, и если за время озвучки читатель
  открыл другое слово, `restore()` молча ничего не делает.
- **«Тап вне» перестал считать плашку чужим элементом** — иначе «стоп»
  закрывал бы тултип вместо возврата (рядом с прежними исключениями
  `.word-tip`, `.bw`, `.rd-viewport`).
- **`js/reader.js`** — `speakBarShow` / `speakBarHide` / `speakBarPlace`.
  Край выбирается сравнением расстояний: `r.top - vp.top` против
  `vp.bottom - r.bottom`, побеждает больший — то есть тот, что **дальше**
  от `.bs--playing`.
- **Плашка `position: fixed` поверх**, как панели в 2C: размеры
  `.rd-viewport` не меняются, значит перепагинация отсюда невозможна.
  Живёт внутри `.rd-view`, поэтому слой `--rd-*` до неё доходит и тема
  ридера применяется сама, без зеркалирования как у тултипа в `body`.
- **Сценарии**: предложение → сворачиваем; «🔊 страница» → `tipClose()`,
  тултип закрывается совсем, остаётся плашка со «стоп»; слово → не
  сворачиваем, озвучка короткая.
- `speakStop()` дополнительно гасит плашку и зовёт `restore()`: если
  очереди не было, `onDone` не придёт, и плашка осталась бы висеть.
- `onResize()` двигает плашку за вьюпортом до `repaginate()`.
- **`css/reader.css`** — секция 21. Высота минимальная (замер: 39 px),
  индикатор — первые 4 слова с многоточием, одна строка с `ellipsis`.

**Проверено** (headless Chrome, 360×640, книга `bremer`, заглушка `TTS`):

| Проверка | Результат |
|---|---|
| Свернулся / вернулся | `collapsed` true → false; после «стоп» перевод символ в символ тот же, бокс тултипа `{t331, b619, h288}` до и после совпал |
| Плашка против `.bs--playing` | ни одного перекрытия на всех 4 предложениях страницы, включая предложение высотой 457 px (`t83–b540` против плашки `t542–b581`) |
| Выбор края | на предложении `t429–b540` плашка ушла наверх (`t62–b101`), на остальных — вниз |
| `pages` и вьюпорт | 2 → 2 на каждом шаге, вьюпорт `t62–b581` не изменился ни разу |
| «Стоп» на плашке | звук остановлен, плашка скрыта, `.bs--playing` снят, тултип вернулся раскрытым |
| Доиграла сама (3 чанка) | то же самое без участия читателя |
| Озвучка слова | тултип остался развёрнутым, плашки нет |
| «🔊 страница» | `tipOpen: false`, плашка со «стоп» на месте, по концу страницы погасла, страница не перелистнулась |
| Листание во время озвучки | плашка скрыта, подсветка снята |

---

### 6.2 Четыре темы ридера вместо трёх (`89a59f6`)

**Повод.** «Системная» наследует глобальную `data-theme`, а глобальная почти
всегда тёмная — поэтому «Системная» и «Тёмная» **брали один и тот же
`--bg: #0f0e17`** и выглядели копиями друг друга.

**Сделано.**

- `THEMES = ['system', 'sepia', 'light', 'dark']`, подписи «Как в
  приложении» / «Сепия» / «Светлая» / «Тёмная».
- «Светлая» и «Тёмная» жёстко задают свой набор `--rd-*` и глобальную тему
  **не читают вообще**. Значения намеренно разведены с токенами `base.css`:
  светлая — белая бумага `#ffffff` (глобальная светлая лавандовая
  `#f5f4ff`), тёмная — OLED-чёрная `#000000` (глобальная тёмная `#0f0e17`).
- Зеркальные наборы для `.word-tip--book[data-rd-theme=...]`: тултип живёт
  в `body`, слой `--rd-*` до него не доходит.
- У ряда шторки появился `segCls`; ряд тем получает `.rd-seg--grid` —
  сетка 2×2, подписи не ужимаются.
- **Совместимость**: сохранённое `'system'` / `'sepia'` / `'dark'` остаётся
  валидным, `readPrefs()` принимает их как раньше.

**Замеры computed background** (не на глаз):

| Тема ридера | глобальная `dark` | глобальная `light` |
|---|---|---|
| system | `rgb(15,14,23)` | `rgb(245,244,255)` |
| sepia | `rgb(244,236,216)` | `rgb(244,236,216)` |
| light | `rgb(255,255,255)` | `rgb(255,255,255)` |
| dark | `rgb(0,0,0)` | `rgb(0,0,0)` |

4 из 4 различаются при обеих глобальных темах. Тултип — 4 из 4 разных
фона, плашка озвучки — 4 из 4.

| Проверка | Результат |
|---|---|
| Ряд тем на 360 px | 2 ряда по 2, кнопки 159×34, `scrollWidth == clientWidth` у всех четырёх — ни одна подпись не обрезана |
| Старые `dm_reader_prefs` | `{"theme":"sepia"}` применились как `sepia`; мусорное `{"theme":"neon"}` упало в `system`, а не сломалось |
| Пагинация | `pages/page = 2/0` на всех четырёх темах |
| Глобальная `data-theme` | не изменилась |

**Пойманное по ходу.** Первый вариант правила для сетки не сработал: у
`.rd-seg button` **тот же вес селектора**, и правило ниже по файлу
выигрывало — все четыре кнопки остались в один ряд по 77 px. Лечится
селектором из двух классов `.rd-seg.rd-seg--grid button`.

---

### 6.3 Флэшкарты: лишний UI (`f8a9f33`)

**Премиса проверена и оказалась перевёрнутой.** «Сломан один урок, дальше
нормально» — на самом деле `a1/lesson-01` в порядке, а лишний UI лежит в
**семи** оболочках `a1/lesson-02…08`. Разметка флэшкарт в `a1/lesson-01`,
`a2/lesson-10`, `b1/lesson-05` и `b2/lesson-14` совпадает символ в символ;
единственная разница — плейсхолдер счётчика («1 из 10» против «1 из …»).

**Сделано.** Оболочки править нельзя, поэтому чистка живёт в
`js/flashcards.js` и вызывается из `init()` до сборки колоды:

- `dropFlipButton()` — кнопка «🔄 Перевернуть» вместе со своей центрирующей
  обёрткой, в которой сидит и подсказка «или нажмите Пробел». Обёртку
  уносим, только если в ней нет чужих узлов (`#flashcard`, `#cardActions`,
  `#cardDots`, `#cardCounter`, `#lessonComplete`).
- `trimActions()` — в `#cardActions` остаются одни кнопки, **те же самые
  узлы с теми же `onclick`**; «Ты знал это слово?» и «← Не знал | Знаю →»
  уходят вместе с их обёрткой.
- `fixHowto()` — подсказка над карточкой ссылалась на кнопку, которой
  больше нет: «нажми «Перевернуть»» → «нажми на карточку».
- Каждый шаг молча выходит, если цели нет: в 61 уроке чистить нечего.
- `css/exercises.css`: `.card-btn` получил `flex: 1 1 0` и `min-height: 54px`,
  шрифт 16/700.

**Не менялись**: подсчёт, XP, `Progress.markSectionDone`, анимация
переворота, сигнатура `init()`. Тап по карточке и `Space` / `←` / `→`
работают как раньше, видимых подсказок про клавиши больше нет.

**Проверено** (360×640, тёмная и светлая тема, колода до конца):

| Проверка | Результат |
|---|---|
| `a1/lesson-02` | не осталось ни кнопки, ни слова «Перевернуть», ни «Пробел», ни «Ты знал это слово», ни стрелок; в `#cardActions` ровно два `BUTTON.card-btn` |
| `.fc-howto` | «…вспомни перевод → нажми на карточку → оцени себя» |
| Кнопки | 151×54 каждая, 151 + 12 + 151 = 314 = вся ширина секции, один ряд |
| Переворот | тап по карточке переворачивает, `Space` переворачивает обратно |
| Колода | проходится до конца, второй проход по «Не знал» работает, карточка и счётчик скрылись, `markSectionDone('flashcards', 100)` |
| XP | `a1/01` 25 слов → **+150** (25×5 + 25), `a1/02` 26 слов → **+155** — ровно как раньше |
| Консоль | 0 ошибок на `a1/01`, `a1/02`, `a2/10`, `b1/05`, `b2/14` |

**Пойманное по ходу.** Первый вариант комментария содержал путь с
последовательностью «звёздочка-слеш» — она **закрыла блочный комментарий**,
и весь `flashcards.js` падал с `SyntaxError: Unexpected identifier`.
Флэшкарты молча не инициализировались во всех 68 уроках. Поймано только
потому, что прогон колоды сверялся с реальным DOM, а не с видом страницы:
`#cardDE` остался «…», счётчик — «1 / 10». Отсюда правило: в комментариях
этого проекта пути писать как `lessons/<lvl>/lesson-NN`, без звёздочки
перед слешем.

---

### После всех трёх

- `python bump_version.py` → **?v=53 → 54, CACHE v68 → v69**, изменён 71
  файл из 72 (68 оболочек + `index.html` + `404.html` + `books.html`, плюс
  `service-worker.js`).
- `python scripts/validate_lessons.py` — 0 ошибок, 0 предупреждений.
- `python scripts/validate_books.py` — 0 ошибок (bremer: 4 главы,
  45 предложений, 1182 токена, 501 словоформа, 376 лемм).

### Не сделано

- **Деплой API на Vercel не выполнен.** CLI установлен (54.18.6), но
  `vercel whoami` отвечает `Error: The specified token is not valid. Use
  vercel login`. Логин интерактивный, поэтому шаг пропущен: ветка
  `_merge_books` в `api/webhook.py` из 5B **на проде по-прежнему не стоит**.
  После `vercel login` останется `make deploy-api` и два смоук-теста:
  `/api/tts?text=Hallo` → 200 и запись старого снимка без поля `books`
  в `/api/progress` → не 500.

### Следующее

- Экран сборника слов (список, фильтр «новые / выученные», переключение
  `status`).
- Вторая книга в библиотеке.

---

## Этап 7 — карточки книги и словарь ✅ (этот коммит)

Данные для этого уже собирались с 3B (`dm_book_words:<id>`, ключ — лемма).
Этап 7 — интерфейс к ним. SRS не делался: `ts` и `status` в записи уже
есть, интервалы подключатся позже без миграции.

### 1. Рефактор `js/flashcards.js` — два режима, один движок

- **`init()` без аргументов** — режим урока, поведение не изменилось ни на
  йоту: колода из `LESSON_DATA.vocabulary`, `TTS.speak` на перевороте,
  `Progress.addXP(5)` за «Знаю», `+25` и `Progress.markSectionDone(
  'flashcards', computeScore())` в конце, второй проход по «не знал».
- **`init({ cards, ids, onDone, showProgress, onGood, onAgain })`** —
  произвольная колода на произвольных DOM-id. Здесь нет ни XP, ни
  `markSectionDone`, ни баннера «Урок завершён»: итог уходит в `onDone`.
- **`ids`** мержится с `DEFAULT_IDS`, значение `null` = узла нет и шаг
  пропускается. Так на обороте карточки книги появляются грамматика
  (`meta`) и помета «устар.» (`old`), которых в уроках нет вообще.
- **`destroy()`** снимает `keydown` и `click` по ссылкам на обработчики и
  обнуляет состояние. `init()` начинается с `destroy()` — повторная
  инициализация не копит слушатели. Проверено отдельно: один Пробел даёт
  ровно один переворот и одну озвучку.
- **Переворот переиспользован целиком**: `.flashcard`, `.flashcard.flipped`,
  `.card-face` из `css/exercises.css`. Ни одного нового правила анимации;
  всё новое — новыми классами (`.fc-meta`, `.fc-old`, `.fc-say`, `.fc-done`).
- Озвучка в произвольном режиме модулем **не делается вообще** — кнопки 🔊
  у вызывающей стороны, через `js/reader-speak.js`.
- `«Повторить»` в произвольном режиме возвращает карточку в конец очереди
  **один раз**: иначе сессия не кончилась бы никогда и «… на повторение»
  нечего было бы считать.

### 2. Кнопка «🃏 карточки (N)» в `#rdSlot` — область ГЛАВА

Выбрана глава, не страница. На 360×640 глава занимает две страницы, а на
десктопе умещается в одну («стр. 1 / 1»): привязка к странице означала бы
на каждом устройстве своё — то три слова, то вся глава, и N прыгал бы
под пальцем на каждом листании. Глава от ширины экрана не зависит и
совпадает ровно с тем, что читатель видит подсвеченным в тексте.

`N` = число различных лемм, которые собраны, ещё активны (`status !== 'known'`)
и встречаются в DOM текущей главы. `N = 0` → кнопка `disabled`.
Пересчёт живёт в `markLemma()` — любая смена пометки меняет и набор
активных слов, поэтому счётчик не отстаёт даже внутри сессии карточек.

Словарь книги — отдельной кнопкой 📗 в `.rd-top`: четвёртая кнопка в
нижний слот на 360 px уже не влезала.

### 3. Карточка

- Лицо: артикль + лемма (`der Hund`) — без артикля карточка учила бы
  неправильной словарной форме. Оборот: перевод, часть речи,
  формы/склонение, «устар.» если есть.
- Тап по карточке — переворот; 🔊 на обеих сторонах (`stopPropagation`,
  чтобы тап по кнопке не переворачивал). Цель озвучки берётся из лица
  карточки в DOM, а не из индекса: после возврата в конец очереди порядок
  меняется, и индекс разъехался бы.
- «Знаю» → `ReaderWords.setStatus(lemma, 'known')` + `Reader.markLemma()`:
  слово уходит из активных и подсветка `.bw--seen` гаснет сразу.
  «Повторить» → статус не трогаем, карточка возвращается в конец очереди.
- Итог: «Разобрано: 3 знаю, 1 на повторение», «Продолжить чтение».
- **Ридер не размонтируется.** `.rd-cards` — отдельная секция с
  `position: fixed` (каркас из `.rd-scope`), `.rd-viewport` размеров не
  меняет, перепагинации нет, поэтому страница и якорь переживают сессию
  нетронутыми.

### 4. Словарь книги

Полноэкранный вид, открывается из ридера (📗) и с карточки книги в
библиотеке. Поиск по слову, переводу и встреченным формам; фильтр
«Все / Новые / Знаю»; запуск карточек по видимому списку (число на кнопке
всегда совпадает со списком); экспорт в текст.

- Тап по слову — **тот же `ReaderTip`**, что в ридере. Глоссарий ему
  собирается из самой записи сборника: в словаре глава может быть не
  загружена, а вся грамматика уже лежит в записи.
- Бейдж «новое / знаю» — кнопка: переключает статус в обе стороны, с тем
  же эффектом на подсветку, что и «Знаю» в карточках.
- Экспорт отдаётся **текстом в поле**, а не файлом: в Telegram WebView
  скачивание не работает, а скопировать можно откуда угодно. Копирование
  через `navigator.clipboard` с откатом на `execCommand` и на «выделено,
  копируйте руками».

### Общее решение по теме

Палитра `--rd-*` была объявлена только на `.rd-view`. Оверлеи лежат на
уровне `body` (иначе прятались бы вместе с ридером и не открывались бы из
библиотеки), поэтому блок токенов теперь общий: `.rd-view, .rd-scope`.
Значения не продублированы ни разу — добавлен только второй селектор.

### Проверено (headless Chrome, 360×640, книга `bremer`)

**Уроки — главное, что не должно было сломаться:**

| Урок | Слов | XP | Ожидалось | markSectionDone |
|---|---|---|---|---|
| `a1/lesson-01` | 25 | **+150** | 25×5 + 25 = 150 | `['flashcards', 100]` |
| `a2/lesson-10` | 20 | **+125** | 20×5 + 25 = 125 | `['flashcards', 100]` |
| `b2/lesson-14` | 16 | **+105** | 16×5 + 25 = 105 | `['flashcards', 100]` |
| `b1/lesson-05` | 24 | **+145** | 24×5 + 25 = 145 | `['flashcards', 100]` |

Плюс: счётчик «Карточка 1 из 25», точки по числу карточек, второй проход
по «не знал», карточка и счётчик скрываются в конце, один Пробел = один
переворот и одна озвучка, 0 ошибок в консоли на пяти уроках.

**Карточки и словарь:**

| Проверка | Результат |
|---|---|
| Пустой сборник | кнопка `disabled`, «(0)» |
| Собрано 5 слов тапами | «(5)», подсвечено 16 словоформ |
| Оверлей открыт | ридер жив, `pages` 2 → 2, вьюпорт тот же, счётчик «Карточка 1 из 5» |
| Переворот | перевод, `meta: ['мест.']`, кнопки показались, **озвучек на перевороте 0** (в уроках — 1) |
| 🔊 карточки | сыграно `es` = лемма с лица, карточка не перевернулась |
| «Знаю» | `status: 'known'`, подсвечено 16 → 15, счётчик «(4)» → «(3)» прямо в сессии |
| Точность снятия подсветки | «Знаю» на `es` погасил ровно его 1 форму; `haben` (5 форм) и `ein/eine` (5) не тронуты; возврат в «новое» вернул форму обратно |
| «Повторить» дважды | слово спрошено 2 раза, итог «Разобрано: 3 знаю, 1 на повторение», статус остался `new` |
| Итог сессии | баннера урока в DOM нет, `dm_progress` = `null` — XP не начислялся |
| Возврат в чтение | `page` 1 → 1 (не первая страница), якорь идентичен, `pages` 2 → 2, вьюпорт не изменился |
| Словарь | 5 строк = 5 в сборнике; «Все 5 = Новые 0 + Знаю 5» сходится |
| Поиск | `ein/eine` → 1 строка |
| Тултип в словаре | открылся, заголовок и перевод из записи, кнопки «Предложение» нет (её и не должно быть) |
| Бейдж | «знаю» → «новое», `status: 'new'` |
| Экспорт | 8 строк, шапка «Собрано: 5 слов (1 новых, 4 знаю)» |
| Карточки из словаря | открылись поверх словаря, словарь под ними жив |
| Счётчик в библиотеке | «📗 5 слов» = 5 записей в сборнике |
| Четыре темы | оверлей карточек 4 из 4 разных фона (`rgb(15,14,23)` / `rgb(244,236,216)` / `rgb(255,255,255)` / `rgb(0,0,0)`), лицо карточки 4 из 4, словарь 4 из 4 |
| Раскладка 360×640 | карточка 316×230, кнопки 152+152 в один ряд, всё в пределах 640, горизонтальной прокрутки нет |
| Ошибок в консоли | 0 за весь прогон |

### Пойманное по ходу

- **Счётчик на кнопке отставал на всю сессию карточек**: `«Знаю»` снимал
  подсветку, но `refreshCards()` звался только на закрытии оверлея. Лечится
  переносом пересчёта внутрь `markLemma()` — смена пометки у леммы и есть
  то самое событие, после которого набор активных слов другой.

### После

- `python bump_version.py` → **?v=54 → 55, CACHE v69 → v70**, 71 файл из 72;
  в оболочках диф только по `?v=`.
- `js/reader-cards.js` и `js/reader-dict.js` добавлены в `STATIC`
  service worker'а.
- `validate_lessons.py` — 0 ошибок, 0 предупреждений.
- `validate_books.py` — 0 ошибок.

### Следующее

- SRS: интервалы поверх `ts` / `status`, миграция не нужна.
- Вторая книга в библиотеке.
- Деплой API из этапа 6 закрыт (`vercel --prod`, прод на новом коде).
